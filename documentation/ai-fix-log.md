# AI fix log — Phase B

> Populated by the Phase B agent run, per the instructions in
> `documentation/prompt.md` §5.5.

---

## 1. Baseline failure (before any edits)

```
<paste the verbatim output of scripts\run_tests.ps1 from the cold repo here>
```

---

## 2. Fixes applied

For each change, one short paragraph:

- **File:** `<path>`
  **What changed:** `<one or two sentences>`
  **Why:** `<which failing test this addresses>`

---

## 3. Final passing state

```
<paste the verbatim output of scripts\run_tests.ps1 after the fixes here>
```

```
<paste the verbatim output of scripts\demo.ps1 here>
```

First 30 lines of `integration/out/ticks.anon.json`:

```
<paste here>
```

---

## 4. Reflection (5–10 sentences)

`<what the failure messages told you, what the test design surfaced, what
would you do differently next time, what made the single prompt effective
or not>`
