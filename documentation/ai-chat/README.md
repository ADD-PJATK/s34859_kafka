# AI chat exports for AA4

This folder collects the full AI conversation history for AA4, per the
assignment spec (Section 12).

| File | Tool | Phase |
|---|---|---|
| _to be added at end of session_ | Claude Code (CLI) | Phase A scaffolding + Phase B one-shot agent run |

Format: the Claude Code transcript will be exported as Markdown
(`session.md`) and committed here before the final push.

Redaction policy: any API keys or personal data found in the transcript
are scrubbed before commit. The AA4 repo has none at the time of
writing.
